# Basic M15 Local MSE Pack

This pack points Homebrew Forge at Kyle's local download of `MagicSetEditorPacks/Basic-M15-Magic-Pack`.

It is the current preferred MTG-style pack because it provides separate blank M15 frame images, PT-box images, and mana-symbol images instead of baked full-card mockups.

Local source:

```text
/Users/kyle/Documents/My Games/Magic The Gathering/Basic-M15-Magic-Pack-main
```

Notes:

- Keep this as `user_supplied_private_use`.
- Do not commit copied frame or symbol image files into this repo.
- DEMO set symbol fallback currently uses Kyle's local `mtg-vectors-main` checkout.
- The renderer composites placeholder art over the frame because these MSE frame JPGs include a black art well.
